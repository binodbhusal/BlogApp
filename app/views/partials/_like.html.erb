<%= form_with(model: [@post, @post.likes.new], local: true) do |form| %>
    <%= form.hidden_field :user_id, value: current_bloguser.id %>
    <%= form.submit "Like", class:"text-yellow bg-dblack px-3 rounded-lg mb-1 hover:text-grey" %>
  <% end %>